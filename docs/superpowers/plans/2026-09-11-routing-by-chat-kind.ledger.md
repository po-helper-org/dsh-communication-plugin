# Леджер выполнения — маршрутизация по виду чата

## Задача 1 — DONE (1c38c72 + f631c9a)
- Important (plan-mandated), исправлено контроллером: комментарий к chatKind обещал то, чего заявка ещё не несла — f631c9a.

## Задача 2 — DONE (41b3982), Approved
- Minor: src/routing.ts:32-35 — при одном чате и в ROUTE_FEED, и в ROUTE_DIALOG побеждает feed по порядку проверок; тай-брейк не задокументирован.
- Minor: src/routing.ts:11 — BY_KIND объявлен Partial, хотя покрывает все ChatKind; смысл Partial — страховка на рост ChatKind, стоит однострочного комментария.

## Задача 3 — DONE (7088f63), Approved
- Minor: src/store.ts:152 — route.join('\n') даёт '' для пустого массива, тогда как links/labels пишут null; сегодня недостижимо (routeFor не возвращает []), латентная несогласованность.
- Minor: src/collector.ts:9-10 — два import из одного модуля, можно слить в один.

## Задача 4 — DONE (45fe052), Approved
- Minor: src/store.ts:197 — GROUP BY включает chat_title и chat_kind; переименованный чат разложится на две строки распределения. Сегодня заголовок задним числом не обновляется, потому безвредно.

## Задача 5 — DONE (d3ba37a + fix), Needs fixes → исправлено контроллером
- Important (plan-mandated), исправлено: ключ <tr> не включал chatTitle/chatKind, хотя store группирует по ним — коллизия при переименовании чата. Ошибка плана.
- Minor: routes-состояние не сбрасывается при открытии раздела — старая таблица видна до ответа; повторяет существующий паттерн page/reload.
- Minor: loadRoutes глотает ошибку без setError, в отличие от reload/openCard.
- ⚠️ Человеку: визуальная проверка блока «Разметка потока» в браузере (шаг 5 задачи 5).

## Финальное ревью ветки (opus) — With fixes → исправлено (d72ce86)
Исправлено до мержа:
- Important: <section> распределения без потолка высоты вытеснял колонки Inbox — flex:none, max-height 35%, overflow-y.
- Important: отказ routes выглядел как «заявок нет» — setError по образцу reload/openCard.

Follow-up (не блокеры), одним коммитом после мержа:
- Настоящий тест миграции на временном файле со старой схемой + переименовать два теста-однофамильца, которые обещают миграцию, а строят свежую схему.
- store.ts: писать null вместо '' для пустого route (как links/labels) и/или GROUP BY COALESCE(route,'') — NULL и '' сейчас дают две строки «не размечено».
- Panel: кнопка ⟳ обновляет список, но не распределение — добавить loadRoutes().
- README.md: раздел настроек не упоминает ROUTE_FEED / ROUTE_DIALOG / ROUTE_BOTH.
- routing.ts: задокументировать тай-брейк (чат в двух списках — побеждает feed).

На этап 2 (Chatwoot), правка модели до второй реализации порта:
- RouteRow.route: Route[] вместо строки с '+' — формат показа не должен рождаться в хранилище; RouteRow.chatKind: ChatKind | null.
- RouteRowView в панели дублирует RouteRow из model.ts, хотя панель уже импортирует оттуда ItemRow/Thread — можно импортировать напрямую.

Отклонение от спеки, признано оправданным: monoforum → dialog (спека: «уточнить на живых данных»). Дефолт «неизвестное — в диалоги», появится в таблице отдельным видом — там и уточнится.
